import { dark } from '@theme-ui/presets';

const theme = {
  ...dark,
  colors: {
    ...dark.colors,
    primary: '#D61F34',
    secondary: '#A11727',
    textVariants: {
      accent: '#7A1522',
    },
  },
  styles: {
    ...dark.styles,
  },
};

export default theme;
